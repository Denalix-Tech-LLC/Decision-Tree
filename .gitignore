.vercel
.DS_Store
node_modules
*.log

# internal source document - do not publish
*.docx
*.doc
decision-tree-prompt.md

# a horizon photo is a local choice, not something the repo ships
land.jpg
land.png
land.webp

# secrets and local database settings
.env
.env.local
.env.*.local

# a crash dump from a Windows grep, not part of the project
*.stackdump

# a local development database, made by npm run dev:local
.pglite
